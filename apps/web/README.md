# Kaxu Web

Planned responsive Web/PWA Projection for pairing, session status, streaming events, remote commands, approvals, and reconnect recovery.

It must consume the public operation protocol and must not call an Agent provider directly.
