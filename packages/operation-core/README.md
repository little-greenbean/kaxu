# `@kaxu/operation-core`

Planned home of `SessionOperation`, deterministic state transitions, policy ports, and event application rules.

This package has not been implemented yet. It must remain independent of provider SDKs, UI frameworks, transport libraries, and storage engines.
