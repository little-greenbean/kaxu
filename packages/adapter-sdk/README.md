# `@kaxu/adapter-sdk`

Planned SDK for Agent capability discovery, command execution, event normalization, cancellation, and conformance tests.

Provider-specific code belongs in separate adapter packages.
